# rufo.dev

Personal website for Andrea Rufo — Software Designer & Full-Stack Developer.

The project is completely static and has no runtime dependencies or build step:

- `index.html` contains the page content and metadata.
- `css/style.css` contains the responsive light and dark themes.

Open `index.html` directly in a browser or serve the directory with any static web server.

## Deploy

Served by GitHub Pages from the `master` branch root. The `CNAME` file binds the custom domain `rufo.dev`.
